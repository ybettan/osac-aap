---
- name: "cumulus: configure access port using nvue"
  raw: |
    flock /tmp/nvue.lock -c '
      nv set interface {{ port_name }} bridge domain br_default access {{ _vlan_id }}
      nv config diff --quiet 2>/dev/null && nv config apply -y
    '
