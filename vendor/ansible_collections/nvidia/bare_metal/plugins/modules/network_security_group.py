#!/usr/bin/python
# -*- coding: utf-8 -*-
# This file is auto-generated by scripts/generate.py. Do not edit manually.

# SPDX-FileCopyrightText: Copyright (c) 2026 Fabien Dupont
# SPDX-License-Identifier: Apache-2.0

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: nvidia.bare_metal.network_security_group
short_description: Manage Network Security Group resources
description:
- Network Security Group is a security policy that controls the traffic flowing between Instances.
version_added: 1.0.0
author: NVIDIA Bare Metal Manager Dev Team
extends_documentation_fragment:
- nvidia.bare_metal.auth
options:
  description:
    type: str
    description:
    - description parameter.
  id:
    type: str
    description:
    - ID of the resource. Used for lookup.
  labels:
    type: dict
    description:
    - labels parameter.
  name:
    type: str
    description:
    - name parameter.
  network_security_group_id:
    type: str
    description:
    - 'ID path parameter: network_security_group_id.'
  rules:
    type: list
    description:
    - rules parameter.
    elements: dict
    suboptions:
      action:
        type: str
        description:
        - action parameter.
        required: true
        choices:
        - PERMIT
        - DENY
      destination_port_range:
        type: str
        description:
        - destination_port_range parameter.
      destination_prefix:
        type: str
        description:
        - destination_prefix parameter.
        required: true
      direction:
        type: str
        description:
        - direction parameter.
        required: true
        choices:
        - INGRESS
        - EGRESS
      name:
        type: str
        description:
        - name parameter.
      priority:
        type: int
        description:
        - priority parameter.
      protocol:
        type: str
        description:
        - protocol parameter.
        required: true
        choices:
        - TCP
        - UDP
        - ICMP
        - ANY
      source_port_range:
        type: str
        description:
        - source_port_range parameter.
      source_prefix:
        type: str
        description:
        - source_prefix parameter.
        required: true
  site_id:
    type: str
    description:
    - site_id parameter.
  state:
    type: str
    description:
    - Desired state of the resource.
    choices:
    - present
    - absent
  stateful_egress:
    type: bool
    description:
    - Egress rules with protocol and destination ports defined but without source ports defined should automatically be made
      stateful.
  wait:
    type: bool
    description:
    - Wait for the resource to reach the desired state.
  wait_timeout:
    type: int
    description:
    - Timeout in seconds for wait operations.
'''

EXAMPLES = r'''
---
- name: Create a Network Security Group
  nvidia.bare_metal.network_security_group:
    api_url: "{{ api_url }}"
    api_token: "{{ api_token }}"
    org: "{{ org }}"
    state: present
    name: "my-network-security-group"

- name: Delete a Network Security Group
  nvidia.bare_metal.network_security_group:
    api_url: "{{ api_url }}"
    api_token: "{{ api_token }}"
    org: "{{ org }}"
    state: absent
    name: "my-network-security-group"
'''

RETURN = r'''
---
resource:
    description: The resource details.
    type: dict
    returned: when state is present
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.nvidia.bare_metal.plugins.module_utils.common import get_auth_argument_spec
from ansible_collections.nvidia.bare_metal.plugins.module_utils.resource import CrudResource


ARGUMENT_SPEC = dict(
description=dict(type='str'),
id=dict(type='str'),
labels=dict(type='dict'),
name=dict(type='str'),
network_security_group_id=dict(type='str'),
rules=dict(type='list', elements='dict', options=dict(
    action=dict(type='str', required=True, choices=['PERMIT', 'DENY']),
    destination_port_range=dict(type='str'),
    destination_prefix=dict(type='str', required=True),
    direction=dict(type='str', required=True, choices=['INGRESS', 'EGRESS']),
    name=dict(type='str'),
    priority=dict(type='int'),
    protocol=dict(type='str', required=True, choices=['TCP', 'UDP', 'ICMP', 'ANY']),
    source_port_range=dict(type='str'),
    source_prefix=dict(type='str', required=True),
)),
site_id=dict(type='str'),
state=dict(type='str', choices=['present', 'absent']),
stateful_egress=dict(type='bool'),
wait=dict(type='bool'),
wait_timeout=dict(type='int'),
)

RESOURCE_CONFIG = {
    'resource_path': '/v2/org/{org}/carbide/network-security-group',
    'resource_item_path': '/v2/org/{org}/carbide/network-security-group/{networkSecurityGroupId}',
    'id_param': 'networkSecurityGroupId',
    'name_field': 'name',
    'create_schema_fields': ['name', 'description', 'site_id', 'stateful_egress', 'rules', 'labels'],
    'update_schema_fields': ['name', 'description', 'stateful_egress', 'rules', 'labels'],
    'scope_fields': [],
    'ready_statuses': ['Ready'],
    'error_statuses': ['Error'],
    'no_create': False,
    'delete_body_fields': [],
    'version_field': None,
}


def main():
    auth_spec = get_auth_argument_spec()
    auth_spec.update(ARGUMENT_SPEC)
    module = AnsibleModule(argument_spec=auth_spec, supports_check_mode=True)
    CrudResource(module, RESOURCE_CONFIG).run()


if __name__ == "__main__":
    main()
